# TAKO-SEN — Agent向け開発ガイド

TAKO-SEN（`tako-sen`）は、Star Battle系のパズルをベースにした、スマートフォン向けのロジックパズルゲームです。

ゲームのテーマにはデフォルメされたタコを使用します。

名称の「TAKO-SEN」は、タコ（TAKO）と、このゲームのルーツであるStar Battleへの敬意を込めた「戦（SEN）」を組み合わせたものです。

## 設計仕様の参照

ゲームの仕様・設計判断については、実装前に必ず [`docs/DESIGN.md`](docs/DESIGN.md) を確認してください。

特に以下に関係する変更では、`docs/DESIGN.md` を正とします。

- ゲームルール
- 問題生成
- Solver
- Logical Solver
- ヒント
- プレイヤー操作
- ショートカット
- 難易度
- 盤面の視覚表現
- ゲーム進行
- データ保存

詳細なゲーム仕様やUX仕様をこのファイルへ重複して記載しないでください。

実装上の都合と `docs/DESIGN.md` が衝突する場合は、`docs/DESIGN.md` を優先します。

要求された機能が `docs/DESIGN.md` の「設計原則」に反する可能性がある場合は、勝手に実装方針を変更せず、その矛盾を指摘してください。

## 技術構成

基本技術として以下を使用します。

- TypeScript
- Vue 3
- Vite
- Cloudflare Workers + Static Assets
- Vitest

特別な理由がない限り、この構成から外れないでください。

ゲームはバックエンドへの接続なしでもプレイ可能なクライアントアプリケーションとして成立させます。

Cloudflare固有の処理をパズルのコアロジックへ持ち込まないでください。

小規模かつ十分にテスト可能な実装で済む場合は、安易に外部依存を追加しないでください。

## アーキテクチャ

パズルロジックはVue、DOM、Cloudflare Workers、永続化、表示処理から独立させます。

現在の責務分担は以下です。

```text
src/
  core/       盤面モデル・ルール・Solver・Generator・難易度分析・成績・タイマー・保存
  ui/         入力判定・ヒント表示補助・Region 表示補助
  App.vue     画面表示と操作の連携
docs/
  DESIGN.md
  MILESTONE.md
```

`src/core/` と `src/ui/` は現状フラットなファイル構成です。上記は厳密なディレクトリ指定ではありません。

実装上の理由があれば変更して構いませんが、この構成例に合わせるためだけの不要な抽象化やディレクトリは作らないでください。

### Core

`src/core/` は可能な限りpure TypeScriptとして実装します。

例外として `src/core/storage.ts` はブラウザの保存処理を扱います。パズルのルール・Solver・Generator から保存処理を参照せず、保存先へのアクセスは小さなインターフェース越しに行ってください。

Core の純粋なパズルロジックは以下の環境から利用できる状態を維持してください。

- ブラウザ
- テスト
- 将来的なWeb Worker
- 将来的にサーバー側生成を行う場合のCloudflare Workers

Coreの関数は暗黙的なグローバル状態に依存せず、明示的な入力と戻り値を優先します。

問題生成など乱数を使用するアルゴリズムでは、可能な限りseed指定可能な乱数源を注入できるようにし、不具合を再現可能にしてください。

### UI

Vueコンポーネントは主に以下を担当します。

- 描画
- ユーザー操作
- アニメーション
- アクセシビリティ
- Coreの結果をプレイヤー向け表示へ変換すること

ゲームルールの正となる実装をVueコンポーネントへ置かないでください。

UIからCoreへ「この操作が可能か」を問い合わせることは構いませんが、同じルールをUI側で再実装しないでください。

### Worker

現状は Cloudflare Static Assets の設定のみで、アプリ固有の Worker 処理はありません。今後追加する場合も Worker は薄く保ちます。

Workersが利用できるという理由だけで、Solverや問題生成をサーバーへ移さないでください。

以下は明示的に要求されるまでスコープ外です。

- D1
- アカウント
- クラウド同期
- オンラインランキング
- サーバー側Daily Puzzle生成

## ドメインモデル

ゲーム上の概念を明示的にモデル化してください。

たとえば以下です。

- board
- cell
- region
- piece
- player mark
- constraint
- logical move
- technique
- hint
- generated puzzle

UI由来の表現をゲーム状態として使用しないでください。

特に以下をゲーム状態の識別子として使用しないでください。

- CSS class
- 表示色
- Vueコンポーネントの状態
- 描画上の座標

RegionのIDと表示色は別概念です。

色をRegionの識別子として扱ってはいけません。

初期ゲームは8×8固定ですが、Coreアルゴリズムの可読性を損なうような不要なマジックナンバー依存は避けてください。

ただし、将来使うか分からない任意サイズ対応のために複雑な汎用化を行う必要もありません。

製品仕様としては8×8です。

## Solverの分離

以下は別の責務として扱います。

### Complete Solver

Complete Solverは数学的な正しさを判定します。

主な責務は以下です。

- 解が存在するか
- 唯一解か
- 複数解か
- 現在のプレイヤー状態が矛盾しているか

探索・バックトラッキングを使用して構いません。

人間らしい解法を再現することより、正確性と十分な性能を優先します。

### Logical Solver

Logical Solverは、人間が理解・適用できる定石をモデル化します。

以下を行えるようにしてください。

- 適用可能な定石を発見する
- なぜその定石が適用できるかを表現する
- 関係するセルやRegionを示す
- 導出される×や確定配置を構造化データとして返す
- brute-forceの結果を人間向けヒントとして偽装しない

Logical Solverは以下で共有します。

- ヒント
- チュートリアル
- 難易度分析
- Generatorの評価
- 必要に応じたショートカットの妥当性確認

各Techniqueは独立してテスト可能にしてください。

Logical Moveには、UI側が推論を再構築しなくても説明・ハイライトできるだけの情報を含めてください。

## Puzzle Generator

問題生成はテスト時に再現可能でなければなりません。

基本 Generator と Logical Solver による初期の難易度評価は実装済みです。今後の変更でも以下の優先順位を維持してください。

1. 正しい8×8問題を生成する
2. 可能なら構築時点で少なくとも1解を保証する
3. Complete Solverで唯一解を確認する
4. 対応済みの人間向け定石のみで解けることを確認する
5. 解法に関する統計情報を収集する
6. 難易度分類の精度・問題の多様性を改善する

基本GeneratorとLogical Solverが十分に安定する前に、高度な難易度推定へ進まないでください。

問題生成は、無作為なRegionを大量に生成して解けるものを探す方法より、既知の正解配置からRegionを構築する方法を優先的に検討してください。

Generator自身の前提とは独立して、生成結果を必ず検証してください。

Generator は指定難易度に一致する候補が見つからない場合、近い評価の候補や curated puzzle にフォールバックします。選択難易度と実際の評価が常に一致する前提で UI やテストを書かないでください。

シードコードには Generator バージョンが含まれます。生成アルゴリズムを変更するときは、同じコードからの再現性と旧バージョンの扱いを確認してください。現状の復元処理は現行バージョンのみを受け付けます。

## テスト

以下には自動テストが必要です。

- ゲームルール
- Complete Solver
- Logical Solver
- 各Technique
- ヒント
- Generator
- 難易度分析
- プレイ時間・一時停止・再読み込み時の復元
- ローカル成績・ランキング・保存

最低限、以下をテストしてください。

- 正しい完成盤面
- 不正な完成盤面
- 隣接禁止
- 行制約
- 列制約
- Region制約
- 解なし
- 唯一解
- 複数解
- 各Logical Technique
- 矛盾したプレイヤー状態
- 誤った×
- Generatorが生成した盤面の妥当性
- Generator生成問題の唯一解
- 同一seedからの決定的な問題生成
- READY 画面・一時停止・再読み込みを挟んでも計時が正しいこと
- 同一クリアの重複記録防止と、未操作の問題をプレイ数へ含めないこと
- 同一シードコードの順位付けと匿名ユーザーセッションの保持

SolverやGeneratorの不具合を修正する場合は、可能な限りその状態またはseedを回帰テストとして追加してください。

ドメインレベルで検証できる内容について、アニメーション時間などのUI実装詳細へ依存するテストは避けてください。

## 入力と操作

入力仕様は `docs/DESIGN.md` を参照してください。

ジェスチャー認識と、それによって実行されるゲーム操作は分離してください。

たとえばpointer event内にタコ配置ルールを書くのではなく、

```text
long press
    ↓
commit piece
    ↓
Coreへ操作要求
```

のように変換してください。

スマートフォンのタッチ操作を第一に設計します。

開発・デバッグ用としてマウスでも十分操作できるようにしてください。

テキスト選択、コンテキストメニュー、ズーム等のブラウザ標準操作を抑制する場合は、ゲーム操作を妨げる箇所だけに限定してください。

アクセシビリティを理由なく広範囲に無効化しないでください。

## アクセシビリティと視覚設計

Regionの色分けは重要なUXですが、ゲーム状態を色だけに依存させてはいけません。

以下を維持してください。

- 明確なRegion境界
- 隣接Region間の十分な識別性
- タコと×の明確な表示
- 誤答マークの識別性
- 適切なフォーカス・操作状態

Region色を完全なランダムで独立して割り当てないでください。

隣接Regionとの識別性を考慮してパレットを割り当てます。

アクセシビリティ改善によって、盤面が過度に騒がしくならないよう注意してください。

## 永続化

初期版ではローカル保存のみを使用します。

保存対象は以下です。

- プレイ中の盤面
- クリア履歴
- ヒント使用数
- ミス数
- READY・一時停止状態と経過時間
- 匿名のローカルユーザー ID とプレイ履歴

設定の永続化は未実装です。設定を追加するときは同じ方針で保存してください。

永続化処理は小さなインターフェースの背後へ分離し、将来的に保存方式を変更できるようにします。

プレイ中状態と完了済みの結果は分けて保存します。タイマーと成績を変更するときは、`docs/DESIGN.md` のクリア・統計・永続化の仕様を確認し、境界条件を回帰テストにしてください。

明示的な要求がない限り、アカウントやクラウド同期を導入しないでください。

## パフォーマンス

盤面は8×8と小規模です。

まず単純で正しいアルゴリズムを優先してください。

ただしGeneratorは多数の候補盤面を評価する可能性があります。

SolverやGeneratorをVueのリアクティビティ等へ不要に結合せず、実測して問題が確認されてから最適化してください。

モバイル上で問題生成が重くなった場合は、サーバー移行より先にWeb Workerの利用を検討してください。

## コード品質

以下を優先します。

- 小さく責務の明確なモジュール
- 明示的な型
- パズル状態変換におけるpure function
- 可能な範囲でimmutableな戻り値
- パズルの概念に基づいた命名
- 構文ではなく、非自明な推論を説明するコメント

以下を避けてください。

- 過剰なフレームワーク抽象化
- 不要なDI framework
- ゲームルールの重複実装
- magic coordinate
- Coreと表示処理の密結合
- テストやコメントにしか存在しないゲームルール

変更を完了と判断する前に、format、type check、関連テストを実行してください。

## スコープ管理

基本ゲーム、難易度評価、ローカル成績までの初期プロトタイプは実装済みです。次の改善対象は [`docs/MILESTONE.md`](docs/MILESTONE.md) を確認し、`docs/DESIGN.md` の設計原則に沿って品質を上げてください。

以下を先回りして実装しないでください。

- アカウント
- 全ユーザー共有ランキング
- 課金
- 広告
- 消費アイテム
- ソーシャル機能
- D1永続化
- サーバー基盤を必要とするDaily Challenge
- 高度な難易度システム
- 追加ゲームルール
- 複数盤面サイズ

合理的な将来拡張を意図的に妨げる必要はありませんが、将来使うかもしれないという理由だけでプロトタイプを複雑化しないでください。

判断に迷った場合は、`docs/DESIGN.md` の設計原則を維持できる最も単純な実装を選択してください。
